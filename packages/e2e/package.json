{
  "name": "@diffect/e2e",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "pnpm --filter @diffect/shared build && pnpm --filter @diffect/core build && pnpm --filter @diffect/web build && playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "1.61.0"
  }
}
